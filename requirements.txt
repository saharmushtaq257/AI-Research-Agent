# Pinned to majors that work together as of Sept 2026.
# Do NOT add `openai` or `litellm` yourself — crewai pins them, and adding
# your own version is the #1 cause of dependency conflicts on Streamlit Cloud.

crewai>=1.15.0,<2.0.0
streamlit>=1.40.0
ddgs>=9.0.0
requests>=2.32.0
beautifulsoup4>=4.12.0
python-dotenv>=1.0.0
